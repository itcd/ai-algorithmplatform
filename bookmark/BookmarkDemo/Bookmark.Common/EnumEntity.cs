using System;
using System.Collections.Generic;
using System.Text;

namespace Bookmark
{
    public enum Privacy { PUBLIC = 0, PRIVATE = 1 }

    public enum OrderBy { ASC = 0, DESC = 1 }
}
